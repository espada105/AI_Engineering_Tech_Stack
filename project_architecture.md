이 레포지토리는 학습을 위한 용도의 공간이다.

루트 기준 최초 진입된 폴더 하나당 도커서버를 갖게 된다.

최초 진입된 폴더에 있는 도커들은 각자의 컨테이너를 띄울 수  있다. compose 파일 자체에서는 현 위치에 존재하는 컨테이너 뿐만이 아닌 다른 컨테이너도 재사용가능하다. 각자의 컨테이너를 띄우지만, 의존성을 분리하여 개별로 컨테이너를 실행할 수 없게 구현이 되어선 안된다. 